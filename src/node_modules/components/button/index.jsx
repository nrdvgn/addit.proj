import React from 'react';
import './Button.sass'

const Button = ({text, onClick, width}) => {
  return <div className='button' onClick={onClick ?? null} style={{width: width ?? 'max-content'}}>{text}</div>;
};

export default Button;
