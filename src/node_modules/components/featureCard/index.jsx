import './FeatureCard.sass'
import defaultFeature from 'assets/img/default-feature.svg'

const FeatureCard = ({icon, title, text}) => {
  return (
    <div className="featureCard">
      <div className="featureCard__icon"><img src={icon ?? defaultFeature} alt="" /></div>
      <p className="featureCard__title">{title}</p>
      <p className="featureCard__text">{text}</p>
    </div>
  );
};

export default FeatureCard;
